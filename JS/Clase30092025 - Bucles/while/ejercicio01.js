/* 1. Desarrolle una función llamada `contarHastaDiez` que imprima en consola los números del 1 al 10 utilizando un bucle `while`. */

/* let contador = 1;

while(contador <= 10){
    console.log(`Numero: ${contador}`);
    contador++
} */

/* 2. Cree una función llamada `contarDesdeDiez` que imprima en consola los números del 10 al 1, en orden descendente, utilizando un bucle `while`. */

/* let contador = 10;

while(contador >= 1){
    console.log(`Número: ${contador}`); 
    contador--;
} */

/* 3. Implemente una función `mostrarParesHastaN` que reciba un número entero `n` como argumento y muestre en consola todos los números pares desde 2 hasta `n` (inclusive), empleando un bucle `while`. */

/* let numero = parseInt(prompt('Ingrese un número: '));
let numInicial = 2;

while (numInicial <= numero){
    if(numInicial % 2 == 0){
        console.log(`Número: ${numInicial}`); 
    }
    numInicial++;
} */

/* let numero = parseInt(prompt('Ingrese un número: '));
let numInicial = 2;   

while (numInicial % 2 == 0 && numInicial <= numero){
    console.log(`Número: ${numInicial}`);
    numInicial = numInicial + 2;
} */